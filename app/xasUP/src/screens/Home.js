import React, {Component, useEffect, useState} from 'react';
import {
  View,
  Text,
  ScrollView,
  StyleSheet,
  TouchableOpacity,
  Dimensions,
  NativeModules,
} from 'react-native';

export default function Home(props) {
  return (
    <View style={{flex: 1}}>
      <Text> Aloo</Text>
    </View>
  );
}
